# 实验 1：课程基础与实验环境搭建

## 一、实验目的

1. 熟悉编程、版本控制和集成开发工具的基本用法。
2. 理解从需求、实现、测试到版本提交的软件工程基本流程。
3. 建立可追溯的实验仓库，为后续课程设计做好准备。

## 二、实验环境

| 类别 | 工具与版本 | 用途 | 验证结果 |
|---|---|---|---|
| 编程语言 | Python 3.13.15 | 编写和运行程序 | 正常 |
| 版本控制 | Git 2.50.1 | 记录代码变更 | 正常 |
| IDE | Visual Studio Code | 代码编辑与调试 | 命令行入口可用 |
| 测试工具 | unittest（Python 标准库） | 自动化测试 | 2 项测试通过 |
| 建模方式 | Mermaid / Markdown | 后续绘制流程图与结构图 | 无需额外安装 |

## 三、实验内容与过程

### 1. 建立实验仓库

创建目录 `software-engineering-experiment-1`，执行 `git init -b main` 初始化 Git 仓库，建立 `.gitignore` 与项目说明，并进行首次提交。

### 2. 编写 Hello World 程序

在 `hello.py` 中将问候语封装为 `greeting()` 函数，由 `main()` 负责输出。这样既满足最小程序要求，也便于独立测试核心行为。

运行命令：

```bash
python3 hello.py
```

实际输出：

```text
Hello, World!
```

### 3. 编写并运行测试

测试覆盖两类行为：

- `greeting()` 的返回值必须为 `Hello, World!`；
- 从命令行运行程序时，标准输出必须为 `Hello, World!` 并正常退出。

执行命令：

```bash
python3 -m unittest discover -s tests -v
```

测试结果：

```text
Ran 2 tests
OK
```

### 4. 使用 Git 记录实验过程

实验按“仓库初始化—功能实现—文档与计划”拆分提交，使每次提交目标单一、历史清晰。可通过以下命令查看：

```bash
git log --oneline --decorate
```

### 5. 确定课程设计主题

课程设计主题确定为“个人任务管理系统”。系统面向个人学习与日常事务管理，覆盖任务创建、状态跟踪、优先级管理、截止日期提醒和统计汇总等典型需求。详细安排见 `PROJECT_PLAN.md`。

## 四、实验结果

| 检查项 | 结果 |
|---|---|
| Python 程序可运行 | 通过 |
| 输出符合预期 | 通过 |
| 自动化测试 | 2/2 通过 |
| 本地 Git 仓库 | 已建立 |
| 提交记录 | 按阶段保存 |
| GitHub 公开仓库 | 已创建并上传实验文件 |
| 课程设计选题 | 已确定 |
| 初步实验计划 | 已完成 |

## 五、问题与解决办法

1. 当前环境未安装 GitHub CLI，因此先完成本地仓库和全部提交，再通过 GitHub 网页创建公开仓库并上传实验成果。
2. 为避免引入环境差异，Hello World 与测试只使用 Python 标准库，不依赖第三方包。

## 六、实验总结

本次实验完成了开发环境验证、最小程序实现、自动化测试和 Git 提交，形成了一个可运行、可验证、可追溯的基础项目。通过将工作拆分为多个独立提交，初步实践了软件工程中“小步实现、及时验证、持续记录”的工作方式，并为后续个人任务管理系统的需求分析和迭代开发建立了基础。
